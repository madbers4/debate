import 'package:flutter/material.dart';

class DefendantCell extends StatelessWidget {
  String name;

  DefendantCell({super.key, required this.name});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
