import 'package:flutter/material.dart';

class PlaintiffCell extends StatelessWidget {
  String name;

  PlaintiffCell({super.key, required this.name});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
